<% content_for :title, 'プロフィール' %>
<div class="max-w-2xl w-full mx-auto py-8">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-600 font-medium rounded-md text-center" id="notice"><%= notice %></p>
  <% end %>
  <h1 class="font-bold text-3xl mb-6">詳細</h1>
  <%= render partial: 'session', locals: { user: @user } %>
  <div class="mt-8 flex gap-2">
    <%= link_to "トップ戻る', "/", class: "rounded-md px-4 py-2 bg-gray-100 hover:bg-gray-200 text-gray-800 font-medium" %>
  </div>
</div>